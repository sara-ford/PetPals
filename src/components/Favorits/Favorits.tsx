import React, { FC } from 'react';
import './Favorits.scss';

interface FavoritsProps {}

const Favorits: FC<FavoritsProps> = () => (
  <div className="Favorits">
    Favorits Component
  </div>
);

export default Favorits;
